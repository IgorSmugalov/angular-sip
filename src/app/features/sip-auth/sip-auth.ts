import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-sip-auth',
  imports: [],
  templateUrl: './sip-auth.html',
  styleUrl: './sip-auth.scss',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class SipAuth {}
